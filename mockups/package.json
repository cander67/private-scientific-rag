{
  "name": "mml-rag-mockups",
  "version": "0.1.0",
  "private": true,
  "description": "Static HTML/CSS mockups for the private scientific RAG workbench",
  "scripts": {
    "serve": "python3 -m http.server 4173",
    "test": "playwright test",
    "screenshots": "playwright test screenshots.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
